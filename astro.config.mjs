import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://young-professionals-canada.github.io",
  base: "/ypca-sprint-guide",
  output: "static",
  trailingSlash: "always"
});
